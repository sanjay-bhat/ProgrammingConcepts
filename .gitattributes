# Normalize line endings
* text=auto

# Treat notebooks as binary so git doesn't diff base64 image blobs
*.ipynb binary

# Don't count notebooks toward the repo's language stats
*.ipynb linguist-vendored
